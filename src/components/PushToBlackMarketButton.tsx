// src/components/PushToBlackMarketButton.tsx

export default function PushToBlackMarketButton({ onClick }) {
  return (
    <button
      onClick={onClick}
      className="btn-vice-pink mt-6"
    >
      🚀 Push to Black Market
    </button>
  );
}
